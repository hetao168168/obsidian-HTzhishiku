---
tags: [仪表盘]
---

# 知识库仪表盘

> 由 Dataview 动态生成，打开即刷新。

## 最近更新（全部笔记）
```dataview
TABLE file.folder AS 分区, file.mtime AS 修改时间
FROM ""
WHERE file.name != "仪表盘"
  AND !contains(file.folder, "Templates")
SORT file.mtime DESC
LIMIT 15
```

## 各区笔记数量
```dataview
TABLE length(rows) AS 笔记数
FROM ""
WHERE !contains(file.folder, "Templates")
GROUP BY file.folder
SORT file.folder ASC
```

## 待办汇总（含未完成 [ ] 的笔记）
```dataview
TABLE file.folder AS 分区
FROM ""
WHERE !contains(file.folder, "Templates")
  AND file.tasks.completed = false
SORT file.mtime DESC
```
